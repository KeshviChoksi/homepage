/**
 * nav.js – Nav is fixed; no scroll class needed.
 * Kept as module for future nav enhancements.
 * ES6 Module
 */

export function initNav() {
  // Nav is always fixed - no scroll-state changes needed
}
